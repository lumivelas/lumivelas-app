'use client';
import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: "#5A7D3A",
    },
    secondary: {
      main: "#C27A4A"
    },
    error: {
      main: "#B35442"
    },
  }
});

export default theme;
